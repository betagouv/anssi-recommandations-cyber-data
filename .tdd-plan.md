# TDD Plan — Sécurisation des routes API

> Généré le 2026-05-21 | 4 tests unitaires | 0 complétés

## api_jeopardy.py

- [x] `test_securise_la_route_jeopardy()` — POST /api/jeopardy/ sans token → 401
- [ ] `test_securise_la_route_jeopardy_documents()` — POST /api/jeopardy/{id}/documents sans token → 401

## api_evaluation.py

- [ ] `test_securise_la_route_evaluation()` — POST /api/evaluation/ sans token → 401
- [ ] `test_securise_la_route_reformulation()` — POST /api/evaluation/reformulation sans token → 401

## Hors-scope (non testables en TU)

Aucun.

---

**Utiliser `/tdd` pour implémenter les tests dans l'ordre ci-dessus.**
